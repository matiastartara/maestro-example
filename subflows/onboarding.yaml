appId: org.wikipedia
---
# Subflow: dismiss the Wikipedia app onboarding carousel and open the search tab.
# Assumes the app was just launched (see flows/*.yaml, which calls launchApp before this).
- tapOn: "Forward"
- tapOn: "Forward"
- tapOn: "Forward"
- tapOn: "Skip"
- tapOn:
    id: "navigation_bar_item_icon_view"
    index: 2
